using UnityEngine;
using System.Collections;

public class Environment : MonoBehaviour {
	//private float timer = 0;
	
	// Use this for initialization
	void Start () {
		//timer = Time.time + 3;	
	}
	
	// Update is called once per frame
	void Update () {
		/*if (timer < Time.time) {
			Vector3 enemyPos = transform.position;
			enemyPos.x += 100;
			
			GameObject enemy = (GameObject)Instantiate(Resources.Load("Bird"), enemyPos, transform.rotation);
			timer = Time.time + 5;
		}*/	
	}
}
